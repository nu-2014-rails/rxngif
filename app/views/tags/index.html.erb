<h1>Tags</h1>

<p><a href="/new_tag">Add a New Tag</a></p>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
  <% @tags.each do |tag| %>
    <tr>
          <td><%= tag.name %></td>
          <td><a href="/tags/<%= tag.id %>">Show</a>
        </td>
      <td><a href="/tags/<%= tag.id %>/edit">Edit</a></td>
      <td><a href="/tags/<%= tag.id %>/destroy">Destroy</a></td>
    </tr>
  <% end %>
  </tbody>
</table>

